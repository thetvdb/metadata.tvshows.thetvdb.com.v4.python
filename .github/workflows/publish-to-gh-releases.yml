name: Publish to Github Releases

on:
  push:
    tags:
      - '*'

jobs:
  publish:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Insert API tokens
        run: |
          sed s/#*apikey*#/${{ secrets.APIKEY }}/ metadata.tvshows.thetvdb.com.v4.python/resources/lib/tvdb.py
          sed s/#*apikey_with_pin*#/${{ secrets.APIKEY_WITH_PIN}}/ metadata.tvshows.thetvdb.com.v4.python/resources/lib/tvdb.py
      - name: Create a ZIP installable file
        run: |
          zip -r metadata.tvshows.thetvdb.com.v4.python-${{ github.ref }}.zip metadata.tvshows.thetvdb.com.v4.python
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            Version ${{ github.ref }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps
          asset_path: ./metadata.tvshows.thetvdb.com.v4.python-${{ github.ref }}.zip
          asset_name: metadata.tvshows.thetvdb.com.v4.python-${{ github.ref }}.zip
          asset_content_type: application/zip
